kind: pipeline
type: docker
name: default

environment:
  VERSION: 1.0.0

steps:
  - name: publish
    image: plugins/docker
    settings:
      repo: joene/discogssorter
      tags: [ "${VERSION}", "latest" ]
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
