kind: pipeline
type: docker
name: default

steps:
  - name: publish branch
    image: plugins/docker
    settings:
      repo: joene/discogssorter
      auto_tag: true
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
